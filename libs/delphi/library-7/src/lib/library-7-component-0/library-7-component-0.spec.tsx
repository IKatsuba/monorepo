import { render } from '@testing-library/react';

import Library7Component0 from './library-7-component-0';

describe('Library7Component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library7Component0 />);
    expect(baseElement).toBeTruthy();
  });
});
