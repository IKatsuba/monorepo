import { render } from '@testing-library/react';

import Library6Component28 from './library-6-component-28';

describe('Library6Component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library6Component28 />);
    expect(baseElement).toBeTruthy();
  });
});
