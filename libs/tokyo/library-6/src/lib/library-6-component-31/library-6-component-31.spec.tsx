import { render } from '@testing-library/react';

import Library6Component31 from './library-6-component-31';

describe('Library6Component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library6Component31 />);
    expect(baseElement).toBeTruthy();
  });
});
