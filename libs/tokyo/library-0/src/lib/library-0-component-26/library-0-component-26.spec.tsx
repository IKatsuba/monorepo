import { render } from '@testing-library/react';

import Library0Component26 from './library-0-component-26';

describe('Library0Component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component26 />);
    expect(baseElement).toBeTruthy();
  });
});
