import { render } from '@testing-library/react';

import Library0Component28 from './library-0-component-28';

describe('Library0Component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component28 />);
    expect(baseElement).toBeTruthy();
  });
});
