import { render } from '@testing-library/react';

import Library0Component18 from './library-0-component-18';

describe('Library0Component18', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component18 />);
    expect(baseElement).toBeTruthy();
  });
});
