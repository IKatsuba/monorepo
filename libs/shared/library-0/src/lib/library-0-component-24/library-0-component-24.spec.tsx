import { render } from '@testing-library/react';

import Library0Component24 from './library-0-component-24';

describe('Library0Component24', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component24 />);
    expect(baseElement).toBeTruthy();
  });
});
