import { render } from '@testing-library/react';

import Library0Component31 from './library-0-component-31';

describe('Library0Component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component31 />);
    expect(baseElement).toBeTruthy();
  });
});
