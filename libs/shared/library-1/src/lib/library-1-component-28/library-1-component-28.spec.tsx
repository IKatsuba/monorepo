import { render } from '@testing-library/react';

import Library1Component28 from './library-1-component-28';

describe('Library1Component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library1Component28 />);
    expect(baseElement).toBeTruthy();
  });
});
