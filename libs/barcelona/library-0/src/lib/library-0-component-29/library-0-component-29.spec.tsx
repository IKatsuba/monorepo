import { render } from '@testing-library/react';

import Library0Component29 from './library-0-component-29';

describe('Library0Component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component29 />);
    expect(baseElement).toBeTruthy();
  });
});
