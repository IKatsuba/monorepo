import { render } from '@testing-library/react';

import Library0Component27 from './library-0-component-27';

describe('Library0Component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component27 />);
    expect(baseElement).toBeTruthy();
  });
});
