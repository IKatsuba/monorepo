import { render } from '@testing-library/react';

import Library7Component28 from './library-7-component-28';

describe('Library7Component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library7Component28 />);
    expect(baseElement).toBeTruthy();
  });
});
