import { render } from '@testing-library/react';

import Library4Component28 from './library-4-component-28';

describe('Library4Component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library4Component28 />);
    expect(baseElement).toBeTruthy();
  });
});
