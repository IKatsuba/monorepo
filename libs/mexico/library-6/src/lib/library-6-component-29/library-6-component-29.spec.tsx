import { render } from '@testing-library/react';

import Library6Component29 from './library-6-component-29';

describe('Library6Component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library6Component29 />);
    expect(baseElement).toBeTruthy();
  });
});
