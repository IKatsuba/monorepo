import { render } from '@testing-library/react';

import Library0Component22 from './library-0-component-22';

describe('Library0Component22', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component22 />);
    expect(baseElement).toBeTruthy();
  });
});
