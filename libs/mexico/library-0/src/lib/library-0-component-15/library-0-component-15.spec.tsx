import { render } from '@testing-library/react';

import Library0Component15 from './library-0-component-15';

describe('Library0Component15', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component15 />);
    expect(baseElement).toBeTruthy();
  });
});
