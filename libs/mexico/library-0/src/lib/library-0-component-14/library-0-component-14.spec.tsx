import { render } from '@testing-library/react';

import Library0Component14 from './library-0-component-14';

describe('Library0Component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component14 />);
    expect(baseElement).toBeTruthy();
  });
});
