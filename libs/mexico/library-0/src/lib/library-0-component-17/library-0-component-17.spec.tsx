import { render } from '@testing-library/react';

import Library0Component17 from './library-0-component-17';

describe('Library0Component17', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component17 />);
    expect(baseElement).toBeTruthy();
  });
});
