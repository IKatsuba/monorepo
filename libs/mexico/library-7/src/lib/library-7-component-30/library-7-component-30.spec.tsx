import { render } from '@testing-library/react';

import Library7Component30 from './library-7-component-30';

describe('Library7Component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library7Component30 />);
    expect(baseElement).toBeTruthy();
  });
});
