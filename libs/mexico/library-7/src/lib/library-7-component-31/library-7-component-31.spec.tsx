import { render } from '@testing-library/react';

import Library7Component31 from './library-7-component-31';

describe('Library7Component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library7Component31 />);
    expect(baseElement).toBeTruthy();
  });
});
