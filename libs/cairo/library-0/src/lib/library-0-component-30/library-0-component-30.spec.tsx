import { render } from '@testing-library/react';

import Library0Component30 from './library-0-component-30';

describe('Library0Component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component30 />);
    expect(baseElement).toBeTruthy();
  });
});
