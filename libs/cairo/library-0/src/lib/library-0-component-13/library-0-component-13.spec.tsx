import { render } from '@testing-library/react';

import Library0Component13 from './library-0-component-13';

describe('Library0Component13', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<Library0Component13 />);
    expect(baseElement).toBeTruthy();
  });
});
